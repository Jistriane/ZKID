#!/usr/bin/env node
// No-op icon generation placeholder to prevent build failures in development
// Replace with actual icon generation when assets are ready
console.log('[gen-icons] No icon generation configured. Skipping.')